let pessoa: string = 'Aline';
let linguagem: string = 'TypeScript';

function BoasVindas (): string {
    return ( 'Olá ' + pessoa + ', seja bem-vinda(o) ao ' + linguagem + '!' );
};

console.log (BoasVindas());